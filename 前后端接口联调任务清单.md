# 前后端接口联调任务清单

## 任务范围

本清单只处理以下问题：

- 小程序页面使用写死数据、未调用后端接口。
- 后台管理系统使用写死数据、未调用后端接口。
- 不包含端口、部署环境和数据库初始化等其他问题。

## 一、后台管理系统

- [x] 登录：移除任意非空账号密码直接进入的演示逻辑，调用真实登录接口并保存登录令牌。
- [x] 运营总览：将写死的统计数据替换为后台总览接口数据。
- [x] 首页配置：直接配置题型 `homeDisplay/homeSort`，保存立即生效；预览读取小程序 `/api/miniapp/home`，不再单独保存草稿或发布首页。
- [x] 题型管理：接入题型列表、详情、新增、修改和启停接口。
- [x] 题型图片：接入图片上传和题型版本封面保存接口，不再只做浏览器本地预览。
- [x] 题目编辑：接入题目和选项的查询、新增、修改、删除接口。
- [x] 结果规则：接入维度、分数区间和结果文案的查询与保存接口。
- [x] 分类标签：接入分类列表、新增、修改、排序和删除接口。
- [x] 发布管理：接入版本校验、发布和下线接口。
- [x] AI 题库助手：已接入 DeepSeek 任务创建、进度查询、失败重试、题目/结果人工编辑和提交题型库接口；完整发布验收仍需独立验证。
- [x] 数据看板：将写死的统计数字和趋势图替换为后端统计接口数据。
- [x] 系统设置：已开放账号、角色、状态和密码管理；邀请有效期、创建频控等业务配置尚待补齐。

## 二、小程序

### 已调用后端的页面和流程

- [x] 首页：调用 `GET /api/miniapp/home`。
- [x] 单人题型详情：调用 `GET /api/miniapp/tests/{id}`。
- [x] 创建单人答卷。
- [x] 加载答卷和题目。
- [x] 保存答案。
- [x] 提交答卷。
- [x] 加载单人报告。

### 仍使用写死数据或本地模拟的页面和流程

- [x] 首页图片字段：统一读取后端 `coverUrl`，不再读取 `iconUrl`。
- [x] 双人测试详情：根据题型 ID 加载真实题型详情。
- [x] 双人邀请：调用后端创建配对接口，不再由前端随机生成配对码。
- [x] 加入配对：调用后端加入配对接口。
- [x] 配对进度：调用后端查询配对状态接口，不再手动模拟对方已完成。
- [x] 取消配对：调用后端取消配对接口。
- [x] 双人报告：调用后端双人报告接口。
- [x] 测试历史：调用个人报告历史接口，删除写死记录。
- [x] 继续测试：调用当前未完成答卷接口并支持继续或重新开始。
- [ ] 会员页面：明确属于 P1，不作为当前 P0 联调阻塞。
- [ ] 反馈提交：明确属于 P1，不作为当前 P0 联调阻塞。
- [ ] 隐私数据删除：已删除假删除申请入口；阻塞于后端未提供数据删除申请接口。
- [x] 设置页面：已移除需要持久化却没有后端支撑的设置，仅保留协议与客户端版本信息。
- [x] 行为事件上报：接入批量事件上报接口。

## 三、实施顺序

1. 后台登录和统一请求封装。
2. 分类管理。
3. 题型及题型版本管理。
4. 题目和结果规则。
5. 首页题型位置、排序保存与小程序同步。
6. 数据看板。
7. 小程序双人测试。
8. 小程序“我的”和历史记录。
9. 当前范围内的隐私和设置；会员、反馈留待 P1。
10. 行为事件上报。

## 四、验收标准

- 页面初始化数据来自后端接口，不再使用前端写死业务数据。
- 新增、修改、删除、排序、发布等操作刷新页面后仍然有效。
- 后台管理系统保存题型首页位置和排序后，小程序首页能够读取最新配置，无需另行发布首页。
- 小程序单人测试、双人测试、历史记录形成完整的后端数据闭环。
- 前端不再显示“演示模式”“未接后端”“仅本地保存”等提示。
