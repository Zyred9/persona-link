<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.personalink.server.mapper.PairReportMapper">

    <insert id="insertIgnore" useGeneratedKeys="true" keyProperty="id">
        INSERT IGNORE INTO t_pair_report
            (report_no, pair_session_id, result_snapshot, generated_at, deleted)
        VALUES
            (#{reportNo}, #{pairSessionId}, #{resultSnapshot}, #{generatedAt}, 0)
    </insert>

</mapper>
