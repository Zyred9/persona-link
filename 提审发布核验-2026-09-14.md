# 映见你我提审发布核验

本文件对应 2026-09-14 复审后的修复。代码、SQL 文件的完成不等于生产部署和微信后台配置已完成。

## 发布顺序

1. 部署前先完成数据库迁移：旧库尚无头像待审字段时执行 `persona-link-server/sql/add-avatar-audit-20260914.sql`（已执行过不要重复 ALTER）；再执行 `persona-link-server/sql/add-avatar-audit-result-20260914.sql` 建立审核结果表（可重复执行）。新库的 `schema.sql` 已包含两项结构。随后部署后端，确认微信 AppID、AppSecret 及内容安全接口权限可用；审核服务异常时应提示稍后重试，不应保存未审核的昵称或反馈。
2. 已有线上协议执行 `persona-link-server/sql/update-privacy-avatar-storage-20260914.sql`，仅补充阿里云头像存储说明；新环境完整初始化协议可使用 `update-legal-document-20260914.sql`。不要用全量脚本覆盖后台另行维护的协议。
3. 增量 SQL 的 `updated_documents` 应为 1；已补充时再次执行为 0。若正文格式不同导致未命中，在后台协议管理补充阿里云、头像图片和访问地址、存储与展示目的，然后保存发布。
4. 读取 `/api/miniapp/legal-documents/2`，确认阿里云条目已发布、版本递增。微信后台的《用户隐私保护指引》也要按实际处理同步更新，本项目协议不能代替平台声明。
5. 上传修复后的小程序包，按下列流程完成真机核验后提交。

## 微信后台核验

- AppID：`wxff1ba41a8287b988`，确认上传目标、主体、服务类目与“趣味性格测试/双人测试”实际服务一致，小程序备案完成。
- request 与 uploadFile 合法域名包含 `https://persona.sanmao.online`；按实际资源调用方式核验 `https://pgcloud.aitici.com` 的访问及所需域名配置。
- 开发工具已开启 `urlCheck`。真机体验版关闭调试后再次验证，不能依赖“不校验合法域名”选项通过测试。本地 HTTP 开发地址只用于本地调试。
- 隐私声明按实际昵称、头像、复制配对码等能力填写，首次触发时验证微信隐私授权。拒绝非必要授权后仍应能使用不依赖该授权的功能。
- 微信消息推送地址配置为 `https://persona.sanmao.online/api/wechat/message-push`，后台 Token 与服务端 `WECHAT_MESSAGE_TOKEN` 一致；加密模式的 EncodingAESKey 与 `WECHAT_MESSAGE_AES_KEY` 一致，`WECHAT_APP_ID` 为当前小程序 AppID。支持 XML/JSON 推送及加密消息；必须完成地址校验，并用真实头像审核确认回调到达。只开通审核接口而不配置回调会一直处于待审状态。

## 真机验收

- 清除本地数据，拒绝协议后仍可浏览公开首页和题型介绍，不自动创建账号；开始测试、查看个人记录等核心操作仍需同意当前协议。
- 同意并开始测试后创建会话；昵称头像可不填写。
- 注销账号后回到游客首页，不立即重新登录；再次主动进入需要账号的功能时才建立新会话。
- 修改昵称、提交反馈：通过内容可保存，违规内容被拦截，审核接口异常提示重试。
- 单人完成答题可查看报告；双人双方完成后均可查看报告，检查 AI 内容提示。
- 上传头像、复制配对码、协议阅读、历史记录与注销均正常；图片不能破图。
- 头像待审时同时保存昵称或再次上传，不应卡在忙碌状态；上传后立即退出重进，待审状态应恢复并继续刷新。审核明确通过后才替换有效头像，拒绝保留旧头像。

## 广告与双人审核说明

广告已有全局关闭和失败策略，不需要添加审核专用账号或隐藏入口。广告尚未准备好时可通过后台关闭，对所有用户一致生效。启用时核验广告单元有效、完整观看可解锁，以及失败策略的实际表现；失败免费策略目前每用户每天最多 3 次，不是无限兜底。退出未看完的视频不算广告故障。

可将下述真实操作路径填写至提审备注，并按实际广告配置补充说明：

> 本小程序提供娱乐性单人测试和双人测试，不提供医疗诊断或专业心理咨询。可先游客浏览题型，开始测试时同意协议并通过微信登录建立会话，头像昵称选填。单人路径：首页选择单人题型→开始测试→提交答案→结果页。双人路径：账号 A 选择双人题型并完成答题→分享邀请或复制配对码→账号 B 打开邀请并完成答题→双方查看双人报告。双人完整流程需要两个微信账号。注销入口：我的→设置→数据管理→注销账号。

如需演示材料，应录制真实双账号完整流程并随提审材料提供；不使用审核专用放行逻辑代替线上实际功能。

## 本次代码验证

- 服务端使用 JDK 17.0.12 执行 `mvn '-Dmaven.test.skip=false' '-Dtest=WechatMessagePushCodecTest,WechatMessagePushControllerTest,WechatContentSecurityClientTest,MiniappUserServiceImplTest,AvatarAuditResultServiceTest,MiniappProfileWebTest,UserContentServiceTest,MiniappAccountServiceImplTest' test`：71 项通过，0 失败、0 错误、0 跳过。覆盖 XML/JSON 及加密回调、早到结果、重复结论和中断恢复；微信 HTTP、存储边界使用模拟服务，未调用真实内容安全接口或验证 MySQL 并发。
- 小程序全部 26 个自检脚本通过，均输出成功标记；使用 Node 和模拟微信平台。覆盖注销后首页、过期 token 埋点、游客浏览与核心功能授权、协议并发校验、AI 组件及环境域名检查。旧答题恢复脚本中“受邀者交卷后进入等待页”的过时期望已与现行直达双人报告流程对齐。
- 未在生产数据库执行 SQL，未部署服务端或上传微信包，未完成微信后台声明、真机及双账号验收；上述发布步骤仍须执行。
