# 接入微信广告方案

## 一、目标与范围

一期提前完成微信激励视频广告接入。包含广告能力的小程序版本发布、微信广告位审核通过后，运营人员在 Web 后台填写广告位 ID 并开启，即可启用广告，无须修改代码或再次发版。

一期不展示任何会员、支付信息，不开放会员中心、会员标识、有效期、套餐、购买或续费入口；后台也不开放会员管理和支付配置。

一期所有用户按全局广告开关决定是否需要观看广告，不按会员身份区分。历史提出的会员差异化规则留到二期重新确认，不在一期生效。

本文为待实施方案，不表示相关功能已经开发或通过微信审核。广告实际展示和收益取决于账号资格、广告位状态及微信广告填充，不保证每次请求都有广告。

## 二、一期业务规则

| 场景 | 处理方式 |
| --- | --- |
| 广告关闭 | 直接授予本次报告访问权限 |
| 广告开启且报告未解锁 | 用户主动观看激励视频，完整观看后解锁 |
| 报告已解锁或已免费获准查看 | 直接查看，不重复观看广告 |
| 用户主动提前关闭广告 | 不解锁，保留答卷，可再次尝试 |
| 广告加载失败或无库存 | 按后台配置免费放行或提示稍后重试 |
| 后台关闭广告，用户仍停留在旧页面 | 下一次权限或解锁请求按最新配置处理 |

开启广告不得重新锁定此前已经获准查看的报告。测试记录与答题完成后的结果入口采用相同权限规则。

用户完成测试后立即进入结果页面，再异步加载访问策略及必要数据；广告加载不阻塞页面跳转。只有用户主动点击“看广告解锁结果”才播放广告。

## 三、Web 后台设计

### 3.1 配置入口

左侧新增独立菜单“广告配置”，建议放在“首页配置”之后。

广告属于全局运营配置，一期适用于所有题型，不在各题型编辑弹窗中重复配置，不增加按题型覆盖规则。

### 3.2 配置表单

| 字段 | 默认值 | 说明 |
| --- | --- | --- |
| 激励视频广告开关 | 关闭 | 控制新报告是否需要观看广告解锁 |
| 广告位 ID | 空 | 填写微信审核通过的 `adUnitId` |
| 广告加载失败处理 | 免费放行 | 可选“免费放行”或“稍后重试” |

表单固定说明：

> 适用于所有题型的测试结果解锁。同一份结果解锁后不再重复观看广告。用户主动提前关闭广告不会解锁。

校验与权限要求：

- 未填写有效格式的广告位 ID，不允许开启广告。
- 格式校验不能证明广告位归属、审核状态或实际可投放性，仍需真机验证。
- 只有具备配置权限的后台管理员可以修改；服务端执行权限检查，不能仅隐藏前端按钮。
- 失败处理使用明确的数字枚举及中文展示名称，前后端保持一致。

### 3.3 当前生效状态

独立展示服务端已保存配置，不将尚未保存的表单内容当作生效状态：

- 广告关闭：用户直接查看结果。
- 广告开启：未解锁的新结果需要观看广告。
- 最近修改时间、修改人。

该区域仅表示本系统配置状态，不显示未经验证的“微信审核通过”或“广告可投放”。

### 3.4 保存交互

- 修改后点击“保存配置”统一生效，不采用单字段修改立即上线。
- 开启或关闭广告时，复用现有站内二次确认弹窗，不使用浏览器原生确认框。
- 保存成功提示：“已生效，后续结果访问请求将使用新配置。”
- 保存失败保留编辑内容，并展示明确错误。
- 复用已有操作审计，记录修改人、修改时间和配置变更。
- 一期不增加虚假的收益统计或 Web 端“测试广告”按钮，广告通过小程序真机验证。

## 四、代码组织与接口职责

### 4.1 三端职责

| 模块 | 职责 |
| --- | --- |
| `persona-link-admin` | 广告配置读取、编辑、保存及生效状态展示 |
| `persona-link-server` | 配置校验、统一报告权限判断、解锁任务和授权记录 |
| `persona-link-miniapp` | 获取访问策略、播放激励视频、请求解锁及展示报告 |

优先复用现有系统配置存储、权限控制和审计能力。实施前检查实际代码，存在合适能力时不另建重复系统。

后台配置接口使用明确的请求和响应 DTO，不提供可任意读写配置项的通用 Map 接口。客户端仅取得播放所需配置，不取得内部管理信息。

### 4.2 统一报告权限判断

所有报告内容读取入口统一经过后端访问判断，包括答题完成页面、测试记录及报告详情，不能仅靠前端遮挡。

```text
请求查看报告
    ↓
校验登录用户及答卷归属
    ↓
是否已有报告访问权限？
    ├─ 是 → 返回可查看状态
    └─ 否 → 读取最新广告配置
                ├─ 广告关闭 → 持久化访问授权 → 返回可查看状态
                └─ 广告开启 → 返回广告解锁要求
```

建议访问状态采用数字枚举：

| 状态码 | 含义 |
| --- | --- |
| `1` | 可查看 |
| `2` | 需要观看广告 |

仅在需要广告时返回广告位 ID 和一次性解锁任务标识。未授权时不返回完整受限报告内容。

接口职责包括后台配置读取与保存、报告访问策略查询、广告完成提交、广告失败处理及授权后的报告读取。具体路径沿用现有工程命名，实施时确定，避免与已有接口重复。

### 4.3 解锁任务与授权记录

- 解锁任务绑定当前用户和答卷，设置有效期及使用状态。
- 解锁前验证归属、任务有效性和当前访问策略。
- 报告授权按用户与答卷保证唯一，重复请求幂等处理。
- 持久化访问授权及来源，例如广告完成、广告关闭时免费授权、失败策略放行。
- 提交失败后允许安全重试，避免观看结束后因短暂断网重复观看。
- 不信任客户端传入的用户身份、任意答卷 ID 或单独的“已看完”标记。

客户端完整观看回调只能作为业务信号，不是不可伪造的服务端凭证。一次性任务、时效和频率校验只能降低风险，不能证明真实观看。实施时核实当前账号是否具备服务端奖励验证能力；若具备则接入，不凭空假设该能力存在。

“免费放行”必须由后端依据当前配置和任务状态决定，并进行频率限制、异常记录，不能仅凭客户端随意提交一次错误就无条件解锁。该策略仍存在伪造失败的风险，需要在体验与收益之间接受明确取舍。

### 4.4 小程序广告生命周期

- 使用微信激励视频广告能力，监听加载、错误及关闭事件。
- 用户完整观看后才进入正常广告解锁流程，主动提前关闭不按加载失败处理。
- 广告位 ID 从服务端取得，不写死在小程序代码中。
- 广告关闭时不创建广告实例；广告位变化时清理旧监听并重新创建实例。
- 页面离开时清理监听及失效请求，避免重复触发解锁。
- 点击解锁时重新确认后端策略，后台关闭广告后无需重启小程序。
- 防止重复点击导致并发播放或多次提交。

### 4.5 配置生效与异常处理

权限和解锁请求以服务端最新配置为准。若现有配置层有缓存，保存成功后须同步失效或更新，不能仅依赖小程序启动时读取一次。

广告加载失败和业务接口失败应区分：接口超时、权限校验失败不能直接当作广告失败放行。未获得后端授权时不返回受限报告，页面提供重试入口并保留答卷。

## 五、会员与微信虚拟支付预留

### 5.1 一期保留的接入位置

核心预留是统一报告访问判断。二期上线会员后，在该入口增加最终确认的会员权益规则，不在多个页面分别判断会员。

二期支付与广告解耦：

```text
服务端确认支付成功
    ↓
更新会员权益
    ↓
统一报告访问判断读取会员权益
    ↓
按最终业务规则决定直接查看或广告解锁
```

支付不直接操作广告播放，不根据客户端支付成功回调直接开通会员。

### 5.2 一期明确不做

- 不展示会员及支付的任何入口、标识、文案或占位按钮。
- 不开放后台会员管理和支付配置。
- 不接入支付 SDK，不生成支付签名。
- 不创建未使用的支付接口、订单表、套餐表或支付配置。
- 不提前增加虚拟币、余额、自动续费或多套策略抽象。

### 5.3 二期再实施

先确认微信主体、类目和会员商品准入，再增加会员权益、套餐、订单、支付通知、主动查单、幂等发放、补单和退款处理。会员与广告的最终关系需在二期明确后实现，不默认将历史规则反转。

## 六、上线步骤

1. 开发后台广告配置、服务端报告授权和小程序激励视频链路，广告默认关闭。
2. 提交并发布包含广告能力的小程序版本，按微信要求完成相关审核。
3. 开通流量主，创建并取得可用激励视频广告位 ID。
4. 在 Web 后台填写广告位 ID、选择失败策略并保存开启。
5. 真机检查广告展示、完整观看解锁、提前退出及异常处理。
6. 在微信官方提供的广告数据中观察实际展示和收益，不以后台开关状态代替收益验证。

后台配置能够免去更换广告位 ID 时重新发版，但不能绕过微信审核，也不能使未发布广告代码的小程序自动获得广告能力。

## 七、验收清单

- 小程序及 Web 后台均无会员、支付入口或相关信息。
- 未填写广告位 ID 时不能开启；保存失败不会显示为已生效。
- 广告关闭时直接查看并记录授权，开启后不会重新锁定旧报告。
- 广告开启时未解锁报告需要用户主动观看广告。
- 完整观看解锁，提前关闭不解锁，同一报告不重复解锁。
- 测试记录和答题完成入口规则一致，越权访问和直接请求报告内容受到限制。
- 广告失败的两种策略均生效，普通接口失败不误触发免费放行。
- 后台更新开关或广告位后，后续客户端请求使用新配置。
- 断网、重复点击、重复回调、失效任务和页面离开不会造成重复授权或丢失答卷。
- 页面跳转不等待广告加载。
- 分别验证代码构建、接口权限与状态流转、小程序真机广告；不能将静态检查视为真实投放验收。

