# 小程序提审合规检查

> 检查日期：2026-09-14
> 检查范围：`persona-link-miniapp`、`persona-link-server`（基于当前工作区代码，含未提交改动）
> 检查依据：《微信小程序平台运营规范》《小程序用户隐私保护指引》及提审常见问题
> 检查方式：全量交叉扫描敏感 API、同意/登录/资料链路、UGC 内容校验、广告与分享、数据删除能力、打包配置

## 一、结论概览

- 已修复并复核确认：6 类问题（强制登录墙、强制头像昵称、先收集后同意、账号注销缺失、打包残留、后台协议未成年人条款缺失）
- 仍待处理：6 项，其中提审前必做 2 项（隐私接口后台声明、审核流程可行性）
- 具体整改动作见「五、提审前动作清单」

## 二、已解决项（复核确认）

| # | 原问题 | 现状 | 关键位置 |
| --- | --- | --- | --- |
| 1 | 打开小程序必须登录才能使用 | 协议页无需登录；游客可直接浏览首页与题型详情；首次使用核心功能（开始测试、查看记录）时静默 `wx.login` 建立会话，无授权弹窗 | `pages/consent/index.js`、`utils/request.js`（`ensureSessionOrCreate`） |
| 2 | 强制要求设置头像昵称 | 头像昵称改为可选，界面已提示「头像和昵称可选，用于报告署名」；未提供时，静默登录建号即写入默认值（昵称生成「用户+6位随机数字」，头像取后台配置，未配置则留空走吉祥物占位），且资料随时可修改 | `components/account-center/index.wxml:37` |
| 3 | 先收集 openid/头像再同意协议 | 顺序调整为「阅读协议并同意 → 进入小程序 → 核心功能时才静默登录」；校验流程不依赖登录与资料 | `app.js`（`verifyConsent`）、`pages/consent/index.wxml` |
| 4 | 无账号注销入口 | 「数据管理」页新增「注销账号」；服务端新增 `DELETE /api/miniapp/me`，事务内清理测试记录、反馈、埋点，失效全部会话，并匿名化 openid、清空资料；注销后重新登录视为全新账号 | `subpackages/account/pages/privacy/index.js`、`MiniappUserContentController.java`、`MiniappAccountServiceImpl.java` |
| 5 | 开发脚本可能随包上传 | `project.config.json` 已将 `scripts` 目录从打包排除；全仓库无 `console.log`/`debugger` 残留 | `project.config.json` |
| 6 | 后台协议缺未成年人条款及占位符 | 《用户协议》《隐私保护指引》《免责声明》已补充未成年人保护与监护人同意条款、统一运营者为张永红并补全联系方式；清理全部占位符与模板编辑提示；按实际采集对齐（腾讯云、无付费、AI 仅提前生成内容、权限仅剪贴板）。本地库已更新，提审环境执行 `sql/update-legal-document-20260914.sql` | `t_legal_document`、`persona-link-server/sql/update-legal-document-20260914.sql` |

## 三、仍待处理项（按优先级）

### 1.【高】隐私接口未在 mp 后台声明

- 现状：代码仍在调用以下隐私相关能力，但 mp 后台《用户隐私保护指引》必须同步声明，否则新基础库下授权会失败、提审会被拦截：
  - 收集昵称、头像：`components/account-center/index.wxml:20`（`open-type="chooseAvatar"`）、`:32`（`input type="nickname"`）
  - 剪切板：`subpackages/pair/pages/invite/index.js:68`、`subpackages/pair/pages/wait/index.js:89`（`wx.setClipboardData`）
- 说明：代码未使用 `wx.onNeedPrivacyAuthorization`。若后台采用「默认隐私弹窗」模式则无需改代码；若配置了「自定义弹窗」模式，需补充隐私授权逻辑。
- 整改建议：
  1. mp 后台「设置 → 服务内容声明 → 用户隐私保护指引」勾选声明：收集昵称头像、剪切板（如采集设备/日志信息也需一并声明）；
  2. 确认弹窗模式与代码实现一致；
  3. 真机验证首次触发头像/昵称/复制时的隐私授权流程。

### 2.【高】审核员可能无法完成完整流程

- 现状：单人/双人报告默认处于锁定态，需用户主动点击「看广告解锁」；审核环境激励视频广告常无填充，审核员将看不到报告内容。双人测试需要另一方扫码加入并完成作答，审核员单人无法完成。
  - 相关位置：`subpackages/test/pages/result/index.wxml:12`、`subpackages/pair/pages/result/index.wxml:6`、`subpackages/test/utils/report-access.js:60-80`
  - 当前代码中没有审核放行开关或白名单。
- 整改建议（二选一或组合）：
  1. 提审备注中写明演示路径（如提供体验账号、录屏、说明广告在审核环境可能不填充的原因）；
  2. 服务端增加审核白名单/开关，命中时跳过广告解锁（并保留双人演示数据）。

### 3.【中】反馈内容无安全校验（UGC）

- 现状：反馈文本由客户端提交后直接入库，服务端未接入微信内容安全接口（全库无 `msgSecCheck` 调用）。
  - 相关位置：`persona-link-server/.../service/impl/FeedbackServiceImpl.java`；客户端入口 `components/account-center/index.js`（`submitFeedback`）
- 整改建议：服务端在写入前调用 `security.msgSecCheck`（或腾讯云内容安全服务）拦截违规内容；最小实现为提交时校验、命中即拒绝并提示。

### 4.【中】AI 生成内容的使用说明

- 现状：题库内容由 DeepSeek 生成、题型图片由千问/即梦生成，经后台人工采用后发布；对外页面没有「AI 生成」相关标识。
  - 相关位置：`persona-link-server/.../service/impl/AiQuestionBankServiceImpl.java`、`persona-link-server/.../ai/ImageGenerationWorker.java`
- 整改建议：确认小程序类目与 AI 内容相关要求（是否需要 AI 生成标识、算法/生成式 AI 备案说明）；如监管或审核要求，则在相关内容页面或协议中增加说明。

### 5.【低】死 UI：相册权限提示页

- 现状：单人报告页保留了「未获得相册权限」页面，但代码中已无 `wx.saveImageToPhotosAlbum` 等相册能力，属于历史残留，容易引起审核困惑。
  - 相关位置：`subpackages/test/pages/result/index.wxml:35-44`、`subpackages/test/pages/result/index.js`（`permission-denied` 视图）
- 整改建议：删除该视图及 `permission` 别名分支；如后续要恢复保存海报功能，再一并恢复。

### 6.【低】剪贴板写入缺少失败提示

- 现状：`wx.setClipboardData` 未提供 `fail` 回调，用户拒绝剪切板授权时点击「复制配对码」没有任何反馈。
  - 相关位置：`subpackages/pair/pages/invite/index.js:68`、`subpackages/pair/pages/wait/index.js:89`
- 整改建议：补充 `fail` 提示（如「复制失败，请手动记录配对码」）。

## 四、配置与环境注意事项

1. 合法域名：`project.config.json` 中 `urlCheck: false`（开发工具不校验域名），提审前需真机验证以下域名已在 mp 后台配置：
   - request / uploadFile：`https://persona.sanmao.online`
   - 图片（OSS）：`https://pgcloud.aitici.com`
2. 旧资源：`assets/images/home-title.png` 已无代码引用（已被 `packOptions.ignore` 排除、代码改用 `home-title.jpg`），可从仓库删除。
3. 在线客服：当前只有「反馈与举报」文本入口，没有 `open-type="contact"` 在线客服；非强制要求，可按需补充。
4. `sitemap.json` 为全 `disallow`：不影响合规，仅影响微信搜索收录，如需收录再调整。
5. 小程序备案：提审前确认已完成 ICP 备案且主体信息（名称、类目、简介）与实际功能一致。

## 五、提审前动作清单

- [ ] mp 后台配置《用户隐私保护指引》：声明「昵称头像」「剪切板」（及设备信息等），确认弹窗模式
- [ ] 准备提审备注：双人流程演示方式、广告解锁说明（或加审核白名单/开关）
- [ ] 反馈提交接入内容安全校验（`msgSecCheck` 或等效方案）
- [x] 后台协议内容补充未成年人条款与联系方式，并复核隐私条款与实际采集一致（2026-09-14 完成；提审前请在后台复核最终文本，同时确认提审环境已执行 `sql/update-legal-document-20260914.sql`）
- [ ] 确认 AI 生成内容的标识/备案要求，必要时补充说明
- [ ] 删除相册权限残留页面；为剪贴板补充失败提示
- [ ] 真机验证域名、隐私弹窗、游客浏览 → 静默登录 → 答题 → 看广告解锁报告全流程
- [ ] 验证账号注销：注销后数据清空、会话失效、重新登录为全新账号

## 附录：验证方式与结果

- 小程序侧 26 个自检脚本：24 个通过；`env-check.js`（期望 IP 与本地 `config/env.js` 不一致）、`assessment-recovery-check.js`（既有断言失败）为本次合规检查之前就存在的问题，与合规项无关。
- 服务端：主代码与测试编译通过；`MiniappAccountServiceImplTest`、`MiniappUserServiceImplTest`、`UserContentServiceTest`、`LegalDocumentVersionControllerTest`、`ConstructorInjectionTest`、`MiniappProfileWebTest` 全部通过。
