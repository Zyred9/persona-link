# 心动测测 V1 技术方案

## 一、技术选型

| 项目 | 选型 |
|---|---|
| Java | Java 17 |
| 服务端 | Spring Boot 3.5.x、Spring Framework 6 |
| 服务端持久层 | MyBatis-Plus 3.5.17 |
| 数据库 | MySQL 8.x、InnoDB、utf8mb4 |
| 运营后台 | Vue 3、Vite、TypeScript；P0 暂不引入重型 UI/状态库 |
| 微信小程序 | 原生微信小程序 |
| 后台鉴权 | 本地账号、BCrypt、固定角色 |
| Icon 存储 | 开发环境使用可配置本地目录；生产对象存储待部署确认 |
| AI 供应商 | 已接入 DeepSeek，地址、模型、密钥由服务端配置管理 |
| 部署形态 | 模块化单体 |

Spring Boot 3.x 最低支持 Java 17。代码和依赖统一使用 Jakarta EE 的 jakarta.* 命名空间。 

## 二、系统架构

| 工程 | 职责 |
|---|---|
| persona-link-admin | 题型、首页配置、发布和运营数据管理 |
| persona-link-miniapp | 登录、首页、答题、配对、报告和事件上报 |
| persona-link-server | 身份会话、内容版本、AI 题库生成、计分、报告、配对、统计和数据一致性 |

~~~mermaid
flowchart LR
    A[persona-link-admin] --> C[persona-link-server]
    B[persona-link-miniapp] --> C
    C --> D[(MySQL 8.x)]
    C --> E[微信登录 API]
    C --> F[微信数据分析 API]
    C --> G[AI 模型 API]
~~~

persona-link-server 使用模块化单体，不拆分微服务，不引入消息队列或分布式事务。

P0 只交付内容运营、单人/双人测试、基础报告和统计闭环。会员、商品、订单、支付、权益、激励广告解锁及反馈/举报工单统一放入 P1；现有相关 UI 仅作为 P1 设计依据，不产生 P0 实现任务。

## 三、核心数据模型

### 3.1 题型与版本

~~~text
t_category
  └── t_test
        └── t_test_version
              ├── t_score_dimension
              ├── t_question
              │     └── t_question_option
              └── t_result_template
~~~

版本状态：

~~~text
DRAFT(1) → PENDING(3，可选排期) → PUBLISHED(4) → OFFLINE(5) → ARCHIVED(6)
~~~

草稿也可直接发布。独立审核工作流已取消，状态 2 仅保留历史兼容，不再进入；AI 内容的人工检查不属于独立审批状态。

- t_test 保存题型标识、类型和分类；小程序首页使用版本封面 `cover_url`，详情页使用 `detail_image_url`，不再配置未使用的题型 Icon。开发环境上传文件写入可配置本地目录，数据库只保存可访问 URL。
- 展示标题、首页封面（`cover_url`）、独立详情图（`detail_image_url`）、说明、预计时长和单次答题数量保存在 t_test_version。
- 详情图可通过后台题型编辑或 AI 新建题型上传、填写 URL，也可清空；随版本复制和发布生效。小程序单人、双人详情页整图等比例展示，未配置时使用各自内置默认详情图，不回退到首页封面。已有数据库执行 `persona-link-server/sql/add-test-version-detail-image.sql` 增量迁移。
- 已发布版本不可修改；修改题目、计分或结果时创建新版本。
- 同一题型只能有一个 PUBLISHED 版本。
- 每个题型版本作为独立题库；用户点击题型后，服务端按该版本配置的 `draw_question_count` 随机抽题。
- 开始答题时锁定 version_id 和本次抽中的题目，后续发布或刷新页面不影响已开始答卷。
- 发布前校验题型、选项计分归属和结果区间，并验证任意合法标准分只命中一个结果。

### 3.2 计分模型

- 每题支持单选或多选，每题至少 2 个选项，数据库不限制最大数量；小程序建议单题不超过 8 个选项。
- 后台动态维护选项，不得将选项数量写死为 A、B、C、D。
- 单选题：题目绑定一个计分维度；选项配置整数 `score_value`，表示选择该项后增加的分值，不是百分比，各选项分值不需要合计 100%。
- 多选题：选项分别绑定一个计分维度和整数 `score_value`，选中后将该分值累计到对应维度；同时配置最少和最多可选数量。
- 多选题的选项归属即计分分支，例如“喜欢独自思考”归属理性维度、“乐于主动交流”归属社交维度；最终按选中的选项分别累计到对应维度，而不是将所有选项累加到题目自身。
- 若多选项都归属同一维度，则选得越多该维度分越高，仅适用于“符合项越多，特征越明显”的题目。
- V1 不单独配置题目权重；单选题和多选题的影响力均由选项分值决定。
- 反向题直接反向配置选项分值，不增加反向计分字段。

维度原始分和标准分按以下公式计算：

~~~text
维度原始分 = Σ 归属该维度的已选选项 score_value
维度理论最低分 = Σ 每道相关题目在所有合法作答组合中对该维度的最小贡献
维度理论最高分 = Σ 每道相关题目在所有合法作答组合中对该维度的最大贡献
维度标准分 = (原始分 - 理论最低分) ÷ (理论最高分 - 理论最低分) × 100
~~~

单选题的合法作答组合为任一选项；多选题的合法作答组合受最少、最多可选数量约束。非必答题还包含“不选择”这一组合，贡献为 0。服务端在发布校验和报告计分时按同一规则计算每个维度的理论最低分和最高分。

- 服务端使用 BigDecimal 计算，维度标准分保留两位小数。
- 题库有 300 题或 500 题不影响计算方式；每次评测均按该答题会话实际抽中的题目计算理论分值范围。
- 标准分最高的维度为主维度；多个维度同分时，按维度 `sort_no` 从小到大确定主维度。
- 根据主维度和标准分区间匹配 `t_result_template`；区间左闭右开，最高分为 100 的区间包含 100。
- 结果区间不得重叠或缺失，最终必须且只能命中一个结果。
- 报告保存结果编码、各维度标准分和展示内容快照，历史报告不重新计算。

### 3.3 首页配置

~~~text
t_test.home_display + t_test.home_sort
  └── GET /api/miniapp/home
~~~

- 后台直接按题型维护首页位置：0 普通列表、1 焦点位、2 推荐位，并通过 `home_sort` 控制同区域顺序。
- 首页展示修改保存到 `t_test` 后立即生效，不再建立独立首页配置草稿和发布版本。
- 焦点位只能有一个；设置新焦点位时，服务端在同一事务内把原焦点位恢复为普通列表。
- 只有启用且存在 PUBLISHED 版本的题型会返回小程序首页。
- 小程序首页上方展示焦点推荐位和推荐题型；下方展示全部启用且存在 PUBLISHED 版本的题型。
- 首页分类来自全部启用分类，只筛选下方普通题型，不影响上方焦点位和推荐位。

### 3.4 单人答题与报告

~~~text
t_answer_session
  ├── t_answer_session_question
  ├── t_answer_detail
  └── t_report
~~~

答卷状态：

~~~text
IN_PROGRESS → SUBMITTED → REPORT_READY
           ↘ ABANDONED
~~~

- t_answer_session 保存 open_id、version_id 和答卷状态。
- answer_status 只属于 t_answer_session，用于表示整份答卷状态；答题明细、报告等其他表不重复保存该字段。
- 创建答题会话时，服务端从当前发布版本中随机抽取 `draw_question_count` 道题，写入 t_answer_session_question，并生成固定的本次答题顺序。
- 创建答题会话必须携带 `create_request_id`；同一请求号重试返回原答卷，不重复创建。续答直接读取原答卷，不调用新建接口。
- 随机抽题先保证每个计分维度至少有一道可计分题，再从剩余题目中随机补足；单选题按题目维度判断，多选题按选项关联的维度集合判断。
- 抽题数量不足以覆盖全部计分维度、配置数量超过可用题目数或某维度没有可计分题时，禁止发布该题型版本。
- 答题中途刷新或重新进入时直接读取 t_answer_session_question，不重新随机抽题。
- 用户每完成一题，客户端先将该题答案保存到服务端，保存成功后再进入下一题；同一题重新作答时，服务端在一个事务内替换该题原答案。
- 小程序重新启动后仍先进入首页，不强制跳转答题页。登录完成后查询当前用户未完成答卷，首页展示“继续上次评测”入口和 `已完成题数/总题数`。
- 用户点击“继续作答”后进入答题页面，读取原题目快照、顺序和已保存答案，并定位到第一道未完成题目。
- 用户再次进入同一评测类型时，可选择“继续上次评测”或“重新开始”；重新开始时先将原答卷标记为 ABANDONED，再创建新的答题会话。
- 未收到保存成功响应的答案不视为已完成；小程序异常退出后，从服务端最后一次保存成功的位置恢复。
- 未完成答卷不自动过期，保持 IN_PROGRESS，直至用户提交、主动重新开始或主动放弃。
- t_answer_detail 每行保存一个已选选项；单选题每题仅一行，多选题按选项保存多行。
- 提交时校验必答题完整性，并在同一事务内完成计分和报告创建。
- t_report 通过 answer_session_id 关联用户和题型版本。
- 报告保存生成时的结果快照，读取历史报告时不重新计分。
- submit_request_id 和 answer_session_id 唯一约束用于防止重复提交和重复报告。

### 3.5 双人配对

~~~text
发起者答卷 ──→ t_pair_session ←── 受邀者答卷
                         └── t_pair_report
~~~

配对状态：

~~~text
INITIATOR_DONE → PARTNER_JOINED → BOTH_DONE → REPORT_READY
       ↘ EXPIRED / CANCELLED
~~~

- 配对会话锁定 version_id，双方必须使用同一题型版本。
- 双人流程固定为发起者先完成答卷，再创建邀请；不支持未完成答卷提前邀请。
- 双人题型只在发起者创建答题会话时随机抽题；受邀者加入后复制发起者的题目快照和顺序，双方不重复抽题。
- 邀请令牌只保存 SHA-256 哈希，并设置唯一索引和过期时间。
- 同一邀请只能绑定一个 partner_open_id。
- 双方答卷完成后幂等生成一份 t_pair_report。
- initiator_visible_flag 和 partner_visible_flag 分别控制双方记录可见性。
- 接口不返回对方逐题答案。

### 3.6 访问统计

| 指标 | 统计口径 | 数据表 |
|---|---|---|
| 今日 UV | 当日去重 open_id | t_analytics_visitor_day |
| 今日 PV | 当日 page_view 事件数 | t_analytics_event |
| 今日新增 UV | 首次访问时间为当日的 open_id 数 | t_analytics_visitor |
| 近 5 分钟活跃 UV | 最近 5 分钟事件去重 open_id | t_analytics_event |
| 题型使用次数 | 成功新建答卷数 | t_answer_session |
| 微信官方日数据 | 微信日趋势接口返回值 | t_wechat_metric_day |

t_analytics_event 保存短期事件明细，t_analytics_visitor_day 保存长期日聚合。微信官方数据按日同步，只用于 T+1 对账，不作为实时数据源。

广告曝光、点击和收益不会从现有访问事件自动同步；后续接入微信广告报表接口或回调时，应单独设计广告指标表。

### 3.7 AI 题库运营

~~~text
运营填写题型信息、生成要求、题库题数和单次答题数
  → 创建题型草稿和生成任务
  → AI 分批生成题目与选项并展示进度
  → 在“题目与选项”“结果规则”两个页签完成人工审核
  → 完成人工审核并提交题型库
  → 跳转题型管理，首页位置默认为“普通列表”
~~~

- “AI题库助手”固定为四步：填写题型信息、生成题库、审核题目与结果、提交题型库，不再单独生成和确认题库方案。第 3 步包含“题目与选项”“结果规则”两个页签。
- 第一步填写题型名称、单人/双人类型、生成要求、题库题数和单次答题数；提交后创建 t_test、DRAFT 状态的 t_test_version 和 t_ai_generation_task，不直接发布。
- 题目按小批次生成，每批通过格式、数量、重复内容、维度归属和选项整数计分校验后，写入 t_score_dimension、t_question、t_question_option 和 t_result_template。
- AI 返回的 JSON 只作为接口传输格式，校验通过后拆分写入现有关系表，不把整套题库存成 JSON。
- t_ai_generation_task 使用 `current_batch_no`、`total_batch_count`、`completed_batch_count`、`generated_question_count`、`retry_count` 记录实际进度和重试次数，并用 `dimension_generated_flag`、`result_rule_generated_flag` 避免重复生成维度和结果规则。
- 生成失败时保留已完成批次和草稿，任务记录错误原因；后台按任务进度字段和该版本已写入的题目数量展示进度，运营点击重试后从下一批继续。
- 生成完成后任务进入“待人工审核”。第 3 步的“题目与选项”页签左侧展示题目列表，支持编辑和逻辑删除题目；右侧展示当前题目的题干与选项，支持编辑和逻辑删除选项。单选题在题目级编辑计分维度，各选项直接展示并编辑整数分值；多选题在选项级直接展示并编辑计分维度和整数分值。题目删除后其选项一并逻辑删除；选项删除后仍须保证该题至少保留两个有效选项。“结果规则”页签按维度审核和编辑分数区间、结果名称、基础文案、深度文案、分享文案和匹配顺序。
- 点击“完成审核并提交”时执行完整性校验，成功后直接跳转题型管理。提交只表示 AI 草稿已完成审核并进入题型库，不自动发布题型；新题型的首页位置默认为“普通列表”，存在已发布版本后才会出现在小程序首页。
- V1 使用 MySQL 保存任务状态，服务内异步执行并由后台轮询进度；不引入 Redis、消息队列或新的微服务。

第 3 步设计依据：`design/admin-ui/admin-ui-v4-ai-question-bank-step3-review.png`、`design/admin-ui/admin-ui-v4-ai-question-bank-step3-result-review.png`。

## 四、核心流程

### 4.1 微信登录

~~~text
wx.login 获取 code
  → persona-link-server 调用 code2Session
  → 获取 open_id
  → 建立服务端业务会话
  → 小程序携带业务会话访问接口
~~~

- open_id 只接受微信服务端返回值，不接受客户端直接提交。
- open_id 按区分大小写方式存储。
- AppSecret 和 session_key 只在服务端使用，不返回小程序。
- 不获取昵称、头像和手机号。

建库脚本已建立 `t_admin_account` 和 `t_business_session` 数据基线：后台账号密码只允许保存 BCrypt 哈希，业务会话只保存令牌 SHA-256 哈希；`role_type` 固定为 1 管理员、2 内容运营、3 只读查看。登录、会话签发/续期、路由守卫和接口权限仍待实现。

### 4.2 内容发布

~~~text
保存草稿
  → 完整性校验
  → 运营确认内容
  → 立即发布或设置发布时间
  → 事务内切换当前发布版本
  → 记录 t_content_audit_log
~~~

题型发布事务先锁定题型及目标版本，再将旧版本下线、新版本设为 PUBLISHED。任一步失败时整体回滚。首页位置和排序直接保存到 `t_test`，不经过草稿、审核或独立发布。

题型发布前必须校验：`draw_question_count` 大于 0、不超过当前版本可用题目数且足以覆盖全部计分维度；每题至少两个选项；单选题已绑定维度、最少和最多可选数量均为 1，且所有选项均配置整数计分值；多选题的最少、最多可选数量合法且不超过选项数，所有选项均已绑定维度并配置整数计分值；每个维度存在可计分题且理论最高分大于最低分；结果区间覆盖 0～100 且不存在重叠。

### 4.3 随机抽题与答卷提交

~~~text
用户点击题型
  → 查询当前 PUBLISHED 版本及 draw_question_count
  → 创建 t_answer_session
  → 随机抽取配置数量的题目并生成顺序
  → 批量写入 t_answer_session_question
  → 返回本次固定题目列表
~~~

创建答题会话和保存题目快照必须在同一事务内完成；任一步失败时不保留空答卷。题目随机结果只生成一次，断点续答直接返回原题目和原顺序。

~~~text
小程序启动并完成登录
  → 查询当前 open_id 的 IN_PROGRESS 答卷
  → 仍进入首页并展示继续作答入口
  → 用户点击继续作答
  → 返回原题目快照、已保存答案和第一道未完成题目的位置
~~~

每题答案单独保存。服务端按 `answer_session_id + question_id` 校验并替换该题答案，成功后客户端才更新进度并进入下一题。已完成题数按服务端已保存答案的题目数计算，不在答卷表增加冗余进度字段。

~~~text
校验业务会话和 open_id
  → 锁定 t_answer_session
  → 按 t_answer_session_question 校验题目范围、必答题和题型选择数量
  → 读取选项的计分归属和计分值
  → 计算维度标准分并匹配结果区间
  → 写入答卷状态和报告快照
  → 提交事务
~~~

小程序按 `questionId + optionIds[]` 提交答案：单选题 `optionIds` 只能有 1 项，多选题必须满足最少、最多可选数量；服务端校验选项确实属于该题且不重复。客户端不接收选项计分值、计分维度和结果匹配区间。重复提交返回已生成的同一报告。

### 4.4 事件上报

~~~text
小程序生成 event_id
  → 批量上报事件
  → 服务端按 event_id 去重
  → 写入 t_analytics_event
  → 更新 t_analytics_visitor
  → UPSERT t_analytics_visitor_day
~~~

event_time 只用于分析，received_time 作为服务端统计时间。运营总览查询结果需返回统计区间和最后更新时间。

“题型使用次数”直接按成功创建的新 `t_answer_session` 统计；`create_request_id` 唯一约束保证同一创建请求的幂等重试不重复建卷，未完成答卷续答不调用新建接口。`event_type = 3` 仍可用于行为分析，但不作为题型使用次数的权威数据源。

## 五、接口能力

### 5.1 小程序端

- 微信 code 登录并换取业务会话。
- 获取按题型首页位置组织的焦点、推荐和普通题型。
- 查询当前用户未完成答卷，返回评测信息、已完成题数、总题数和继续作答所需的会话标识。
- 获取题型当前发布版本，创建答题会话并返回本次随机抽取的固定题目列表。
- 读取未完成答卷的固定题目、已保存答案和第一道未完成题目的位置。
- 按题保存或替换答案，保存成功后才允许进入下一题。
- 放弃未完成答卷并重新开始同一评测。
- 保存或提交答案、查询报告；答案统一提交 `questionId + optionIds[]`。
- 发起者完成答卷后创建双人配对；受邀者加入配对并查询配对状态和报告。
- 分页查询单人及双人记录，支持逻辑删除个人可见记录。
- 批量上报访问事件。

### 5.2 运营后台

- 分类和题型的分页、详情、保存、更新、批量逻辑删除。
- “AI题库助手”提供题型信息与生成要求填写、生成进度及失败重试、题目与结果审核、提交题型库四步流程。
- AI 生成完成后进入“题目与选项”“结果规则”两个审核页签：题目页左侧题目列表支持编辑和删除，右侧选项支持编辑和删除；单选题在题目级编辑计分维度、在选项级编辑整数分值，多选题在选项级编辑计分维度和整数分值；结果规则页按维度编辑分数区间和完整结果文案。
- 完成人工审核后提交并跳转题型管理，首页位置默认为“普通列表”；不提供 AI 直接发布入口。
- 题型版本、单次答题数量和计分维度维护；后台同时展示题库可用题目数，并限制单次答题数量不得超过题库数量。
- 题目动态增删选项，并维护单选/多选题型和多选题的最少、最多可选数量。
- 单选题配置题目维度及选项整数“计分值”；多选题为每个选项配置计分维度和整数“计分值”；后台不使用“权重百分比”文案。
- 结果模板按维度配置标准分起止区间和匹配顺序。
- 题型版本校验、预览、发布、排期、下线和归档。
- 直接维护题型首页位置和排序，保存后小程序首页立即读取最新数据。
- 查询实时 UV/PV、访问趋势、各题型使用次数和微信官方日数据。
- 查询内容发布审计记录。

## 六、一致性与安全

### 6.1 事务边界

以下操作必须使用数据库事务：

- 题型版本发布，以及切换唯一首页焦点题型。
- AI题库助手第一步提交时创建题型草稿、题型版本和生成任务；AI 生成批次写入计分维度、结果模板、题目和选项。
- 每个 AI 题目批次在同一事务内写入题目和选项；后台按该版本实际题目数计算生成进度。
- 答题会话创建和随机题目快照写入。
- 答卷提交、计分和单人报告生成。
- 受邀者绑定和双人报告生成。
- 用户删除记录时更新对应的可见标记。

### 6.2 幂等与并发

- 答卷提交使用 submit_request_id 唯一索引。
- 答卷创建使用 create_request_id 唯一索引。
- 单人报告使用 answer_session_id 唯一索引。
- 双人报告使用 pair_session_id 唯一索引。
- 事件上报使用 event_id 唯一索引。
- 同一 AI 任务只允许一个生成或重试请求执行，状态不符合时拒绝重复操作。
- 邀请加入时锁定 t_pair_session，并校验状态、过期时间和 partner_open_id。
- 发布操作锁定当前版本，避免同时出现多个 PUBLISHED 版本。

### 6.3 数据安全

- open_id、答卷、报告和配对记录只允许当前登录身份访问。
- AppSecret、业务会话密钥和邀请令牌明文不得写入数据库或日志。
- AI 模型地址、密钥和模型名称由服务端配置管理，密钥不得返回后台或写入日志。
- AI 输出按不可信数据处理，入库前校验字段长度、枚举、题目数量、选项归属和可发布性；AI 草稿必须人工审核。
- 后台接口执行鉴权并记录发布、下线和删除审计。
- 所有配置表和业务表使用 deleted 逻辑删除；历史报告快照不随内容版本变更。
- SQL 使用参数绑定，上传资源校验类型、大小和存储路径。

## 七、异常处理与后台任务

| 场景 | 处理 |
|---|---|
| 微信登录失败 | 不创建业务会话，返回可重试错误 |
| 首页题型配置无效 | 拒绝保存停用或没有已发布版本的推荐题型 |
| 题型无发布版本 | 禁止创建答题会话 |
| 单题答案保存失败 | 停留当前题并允许重试，不提前增加已完成题数 |
| 答卷重复提交 | 返回已有报告 |
| 邀请过期或已绑定 | 拒绝加入 |
| 双人报告生成失败 | 保留 BOTH_DONE 状态，允许幂等重试 |
| AI 生成任务创建失败 | 回滚题型草稿、题型版本和生成任务，返回可重试错误 |
| AI 题库批次生成失败 | 保留已生成题目并记录错误原因，允许从下一批重试 |
| 服务重启中断 AI 任务 | 保留题型草稿，启动后将未完成任务置为生成失败，由运营手动重试 |
| 事件重复上报 | 依赖 event_id 唯一索引忽略重复数据 |
| 微信官方数据同步失败 | 保留旧数据并记录失败状态，下一次任务重试 |

微信官方日数据同步、过期邀请处理和事件明细清理由外部调度服务调用 HTTP Job 接口，不在业务服务中使用 @Scheduled。t_analytics_event 默认保留 30 天，清理任务按日期批量删除。

## 八、数据库设计与建表脚本

### 8.1 数据库约定

- 数据库：MySQL 8.x，存储引擎 InnoDB，字符集 utf8mb4。
- 主键统一使用 BIGINT UNSIGNED；状态、类型等枚举统一保存数字 code。
- 可软删除的业务表统一使用 `deleted`；只有分类和题型保留独立启停所需的 `status`。
- 不重复保存能够通过稳定关联关系推导出的 test_id、version_id 和 open_id。
- 已发布题型版本、已完成答卷、报告快照和审计日志不做原地覆盖。
- 表之间使用逻辑外键，由服务端事务和唯一索引保证一致性，V1 不增加物理外键。
- `open_id` 由服务端通过微信 code2Session 获取，客户端不得自行提交或修改；数据库按区分大小写方式保存。
- JSON 字段只保存结果展示内容、报告快照和审计快照；维度、选项计分值和结果区间使用普通列。

主要枚举约定：

| 字段 | code |
|---|---|
| test_type | 1 单人测试，2 双人测试 |
| version_status | 1 草稿，2 历史保留不再使用，3 待发布，4 已发布，5 已下线，6 已归档 |
| home_display | 0 普通列表，1 焦点位，2 推荐位 |
| question_type | 1 单选，2 多选 |
| ai_task_status | 1 待生成，2 生成中，3 待人工审核，4 生成失败 |
| answer_status | 1 作答中，2 已提交，3 报告已生成，4 已放弃 |
| pair_status | 1 发起者已完成，2 对方已加入，3 双方已完成，4 报告已生成，5 已过期，6 已取消 |
| event_type | 1 小程序打开，2 页面浏览，3 开始测试，4 完成测试，5 分享 |
| status | 1 启用，0 禁用 |
| deleted | 0 正常，1 已删除 |

### 8.2 数据表清单

| 业务域 | 数据表 |
|---|---|
| 分类与题型 | t_category、t_test、t_test_version |
| 题目与结果 | t_score_dimension、t_question、t_question_option、t_result_template |
| 作答与报告 | t_answer_session、t_answer_session_question、t_answer_detail、t_report |
| 双人配对 | t_pair_session、t_pair_report |
| AI 题库运营 | t_ai_generation_task |
| 内容审计 | t_content_audit_log |
| 运营分析 | t_analytics_visitor、t_analytics_event、t_analytics_visitor_day、t_wechat_metric_day |
| 身份与会话支撑 | t_admin_account、t_business_session |

V1 共建立 21 张表，其中 19 张业务表、2 张身份与会话支撑表。

### 8.3 MySQL 8.x 建表脚本

正式数据库结构以 `persona-link-server/sql/schema.sql` 为权威，演示数据以 `demo-data.sql` 为权威；以下 DDL 是同步快照，出现差异时必须先修改建库脚本，再同步本文档。当前结构共 21 张表；演示数据包含 1 分类、1 题型、1 版本、1 维度、1 题、2 选项和 3 条结果规则。

~~~sql
CREATE TABLE IF NOT EXISTS t_category (
    id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键',
    category_name VARCHAR(64) NOT NULL COMMENT '分类名称',
    sort_no INT NOT NULL DEFAULT 0 COMMENT '排序值，越大越靠前',
    status TINYINT NOT NULL DEFAULT 1 COMMENT '状态：1启用，0禁用',
    deleted TINYINT NOT NULL DEFAULT 0 COMMENT '删除标记：0正常，1删除',
    create_date DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_date DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (id),
    KEY idx_category_status_sort (status, deleted, sort_no)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='题型分类';

CREATE TABLE IF NOT EXISTS t_test (
    id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键',
    test_name VARCHAR(100) NOT NULL COMMENT '后台内部题型名称',
    test_type TINYINT NOT NULL COMMENT '测试类型：1单人，2双人',
    category_id BIGINT UNSIGNED NOT NULL COMMENT '分类ID',
    status TINYINT NOT NULL DEFAULT 1 COMMENT '状态：1启用，0禁用',
    home_display TINYINT NOT NULL DEFAULT 0 COMMENT '首页位置：0普通列表，1焦点位，2推荐位',
    home_sort INT NOT NULL DEFAULT 0 COMMENT '首页排序值，越大越靠前',
    deleted TINYINT NOT NULL DEFAULT 0 COMMENT '删除标记：0正常，1删除',
    create_date DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_date DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (id),
    KEY idx_test_category (category_id, status, deleted),
    KEY idx_test_home (home_display, status, deleted, home_sort)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='测试题型';

CREATE TABLE IF NOT EXISTS t_test_version (
    id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键',
    test_id BIGINT UNSIGNED NOT NULL COMMENT '题型ID',
    version_no INT UNSIGNED NOT NULL COMMENT '版本号',
    title VARCHAR(100) NOT NULL COMMENT '版本标题快照',
    cover_url VARCHAR(500) NOT NULL COMMENT '版本封面快照',
    detail_image_url VARCHAR(500) NULL COMMENT '版本详情图快照',
    description TEXT NULL COMMENT '题型说明',
    estimated_minutes SMALLINT UNSIGNED NOT NULL DEFAULT 1 COMMENT '预计答题分钟数',
    draw_question_count SMALLINT UNSIGNED NOT NULL COMMENT '单次随机抽取题目数',
    version_status TINYINT NOT NULL DEFAULT 1 COMMENT '1草稿，2历史保留，3待发布，4已发布，5已下线，6已归档',
    version_note VARCHAR(500) NULL COMMENT '版本说明',
    review_reason VARCHAR(500) NULL COMMENT '历史审核原因，当前流程不使用',
    scheduled_at DATETIME NULL COMMENT '计划发布时间',
    published_at DATETIME NULL COMMENT '实际发布时间',
    offline_at DATETIME NULL COMMENT '下线时间',
    risk_offline_flag TINYINT NOT NULL DEFAULT 0 COMMENT '风险下线：0否，1是',
    deleted TINYINT NOT NULL DEFAULT 0 COMMENT '删除标记：0正常，1删除',
    create_date DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_date DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_test_version (test_id, version_no),
    KEY idx_version_publish (version_status, scheduled_at, deleted)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='测试题型版本';

CREATE TABLE IF NOT EXISTS t_score_dimension (
    id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键',
    version_id BIGINT UNSIGNED NOT NULL COMMENT '题型版本ID',
    dimension_code VARCHAR(32) NOT NULL COMMENT '维度编码',
    dimension_name VARCHAR(64) NOT NULL COMMENT '维度名称',
    sort_no INT NOT NULL DEFAULT 0 COMMENT '展示及同分判定顺序，越小越优先',
    deleted TINYINT NOT NULL DEFAULT 0 COMMENT '删除标记：0正常，1删除',
    create_date DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_date DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_version_dimension_code (version_id, dimension_code),
    KEY idx_dimension_version_sort (version_id, deleted, sort_no)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='题型计分维度';

CREATE TABLE IF NOT EXISTS t_question (
    id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键',
    version_id BIGINT UNSIGNED NOT NULL COMMENT '题型版本ID',
    question_type TINYINT NOT NULL DEFAULT 1 COMMENT '题型：1单选，2多选',
    dimension_id BIGINT UNSIGNED NULL COMMENT '单选题计分维度ID；多选题由选项配置维度',
    min_select_count TINYINT UNSIGNED NOT NULL DEFAULT 1 COMMENT '最少可选数量',
    max_select_count TINYINT UNSIGNED NOT NULL DEFAULT 1 COMMENT '最多可选数量',
    question_no SMALLINT UNSIGNED NOT NULL COMMENT '题号',
    question_text VARCHAR(500) NOT NULL COMMENT '题干',
    required_flag TINYINT NOT NULL DEFAULT 1 COMMENT '是否必答：1是，0否',
    sort_no INT NOT NULL DEFAULT 0 COMMENT '排序值，越大越靠前',
    deleted TINYINT NOT NULL DEFAULT 0 COMMENT '删除标记：0正常，1删除',
    create_date DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_date DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_version_question_no (version_id, question_no),
    KEY idx_question_version_sort (version_id, deleted, sort_no),
    KEY idx_question_dimension (version_id, dimension_id, deleted),
    CONSTRAINT chk_question_select_range CHECK (min_select_count >= 1 AND min_select_count <= max_select_count)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='版本题目';

CREATE TABLE IF NOT EXISTS t_question_option (
    id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键',
    question_id BIGINT UNSIGNED NOT NULL COMMENT '题目ID',
    option_code VARCHAR(16) NOT NULL COMMENT '选项编码',
    option_text VARCHAR(300) NOT NULL COMMENT '选项文案',
    dimension_id BIGINT UNSIGNED NULL COMMENT '多选题选项计分维度ID；单选题为空',
    score_value SMALLINT NOT NULL COMMENT '选项整数计分值',
    sort_no INT NOT NULL DEFAULT 0 COMMENT '排序值，越大越靠前',
    deleted TINYINT NOT NULL DEFAULT 0 COMMENT '删除标记：0正常，1删除',
    create_date DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_date DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_question_option_code (question_id, option_code),
    KEY idx_option_question_sort (question_id, deleted, sort_no),
    KEY idx_option_dimension (dimension_id, deleted)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='题目选项';

CREATE TABLE IF NOT EXISTS t_result_template (
    id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键',
    version_id BIGINT UNSIGNED NOT NULL COMMENT '题型版本ID',
    dimension_id BIGINT UNSIGNED NOT NULL COMMENT '匹配的主维度ID',
    result_code VARCHAR(32) NOT NULL COMMENT '结果编码',
    result_name VARCHAR(100) NOT NULL COMMENT '结果名称',
    score_min DECIMAL(5,2) NOT NULL COMMENT '标准分下限，包含',
    score_max DECIMAL(5,2) NOT NULL COMMENT '标准分上限，除100外不包含',
    basic_result_json JSON NOT NULL COMMENT '基础结果内容',
    deep_result_json JSON NULL COMMENT '深度结果内容',
    share_copy_json JSON NULL COMMENT '分享文案配置',
    sort_no INT NOT NULL DEFAULT 0 COMMENT '匹配顺序，越小越先匹配',
    deleted TINYINT NOT NULL DEFAULT 0 COMMENT '删除标记：0正常，1删除',
    create_date DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_date DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_version_result_code (version_id, result_code),
    KEY idx_result_match (version_id, dimension_id, deleted, sort_no),
    CONSTRAINT chk_result_score_range CHECK (score_min >= 0 AND score_max <= 100 AND score_min < score_max)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='结果模板';
CREATE TABLE IF NOT EXISTS t_answer_session (
    id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键',
    answer_no VARCHAR(32) NOT NULL COMMENT '答卷业务编号',
    create_request_id VARCHAR(64) NOT NULL COMMENT '创建答卷幂等请求号',
    open_id VARCHAR(128) COLLATE utf8mb4_bin NOT NULL COMMENT '微信用户OpenID',
    version_id BIGINT UNSIGNED NOT NULL COMMENT '锁定的题型版本ID',
    answer_type TINYINT NOT NULL COMMENT '答卷类型：1单人，2双人参与者',
    answer_status TINYINT NOT NULL DEFAULT 1 COMMENT '1作答中，2已提交，3报告已生成，4已放弃',
    submit_request_id VARCHAR(64) NULL COMMENT '提交幂等请求号',
    submitted_at DATETIME NULL COMMENT '提交时间',
    report_ready_at DATETIME NULL COMMENT '报告生成时间',
    deleted TINYINT NOT NULL DEFAULT 0 COMMENT '删除标记：0正常，1删除',
    create_date DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_date DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_answer_no (answer_no),
    UNIQUE KEY uk_answer_create_request (create_request_id),
    UNIQUE KEY uk_answer_submit_request (submit_request_id),
    KEY idx_answer_open_time (open_id, create_date),
    KEY idx_answer_version_status (version_id, answer_status, deleted)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='答题会话';

CREATE TABLE IF NOT EXISTS t_answer_session_question (
    id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键',
    answer_session_id BIGINT UNSIGNED NOT NULL COMMENT '答题会话ID',
    question_id BIGINT UNSIGNED NOT NULL COMMENT '本次抽中的题目ID',
    sort_no SMALLINT UNSIGNED NOT NULL COMMENT '本次答题顺序',
    deleted TINYINT NOT NULL DEFAULT 0 COMMENT '删除标记：0正常，1删除',
    create_date DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_date DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_session_question (answer_session_id, question_id),
    UNIQUE KEY uk_session_question_sort (answer_session_id, sort_no)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='答题会话题目快照';

CREATE TABLE IF NOT EXISTS t_answer_detail (
    id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键',
    answer_session_id BIGINT UNSIGNED NOT NULL COMMENT '答题会话ID',
    question_id BIGINT UNSIGNED NOT NULL COMMENT '题目ID',
    option_id BIGINT UNSIGNED NOT NULL COMMENT '选项ID',
    deleted TINYINT NOT NULL DEFAULT 0 COMMENT '删除标记：0正常，1删除',
    create_date DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_date DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_answer_question_option (answer_session_id, question_id, option_id),
    KEY idx_answer_detail_question (answer_session_id, question_id),
    KEY idx_answer_detail_option (option_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='答卷明细';

CREATE TABLE IF NOT EXISTS t_report (
    id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键',
    report_no VARCHAR(32) NOT NULL COMMENT '报告业务编号',
    answer_session_id BIGINT UNSIGNED NOT NULL COMMENT '答题会话ID',
    result_code VARCHAR(32) NOT NULL COMMENT '命中的结果编码',
    result_snapshot JSON NOT NULL COMMENT '生成时的结果展示快照',
    generated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '生成时间',
    deleted TINYINT NOT NULL DEFAULT 0 COMMENT '删除标记：0正常，1删除',
    create_date DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_date DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_report_no (report_no),
    UNIQUE KEY uk_report_answer (answer_session_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='单人测试报告';

CREATE TABLE IF NOT EXISTS t_pair_session (
    id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键',
    pair_no VARCHAR(32) NOT NULL COMMENT '配对业务编号',
    create_request_id VARCHAR(64) NOT NULL COMMENT '创建邀请幂等请求号',
    invite_token_hash CHAR(64) NOT NULL COMMENT '邀请令牌哈希，不保存明文',
    version_id BIGINT UNSIGNED NOT NULL COMMENT '双方共同使用的题型版本ID',
    initiator_open_id VARCHAR(128) COLLATE utf8mb4_bin NOT NULL COMMENT '发起者OpenID',
    initiator_answer_session_id BIGINT UNSIGNED NOT NULL COMMENT '发起者答题会话ID',
    partner_open_id VARCHAR(128) COLLATE utf8mb4_bin NULL COMMENT '受邀者OpenID',
    partner_answer_session_id BIGINT UNSIGNED NULL COMMENT '受邀者答题会话ID',
    pair_status TINYINT NOT NULL DEFAULT 1 COMMENT '1发起者已完成，2对方已加入，3双方已完成，4报告已生成，5已过期，6已取消',
    initiator_visible_flag TINYINT NOT NULL DEFAULT 1 COMMENT '发起者记录可见：1是，0否',
    partner_visible_flag TINYINT NOT NULL DEFAULT 1 COMMENT '受邀者记录可见：1是，0否',
    joined_at DATETIME NULL COMMENT '受邀者加入时间',
    expires_at DATETIME NOT NULL COMMENT '邀请过期时间',
    deleted TINYINT NOT NULL DEFAULT 0 COMMENT '删除标记：0正常，1删除',
    create_date DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_date DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_pair_no (pair_no),
    UNIQUE KEY uk_pair_create_request (create_request_id),
    UNIQUE KEY uk_pair_invite_token (invite_token_hash),
    UNIQUE KEY uk_pair_partner_answer (partner_answer_session_id),
    KEY idx_pair_initiator_time (initiator_open_id, create_date),
    KEY idx_pair_partner_time (partner_open_id, create_date),
    KEY idx_pair_status_expire (pair_status, expires_at, deleted)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='双人配对会话';

CREATE TABLE IF NOT EXISTS t_pair_report (
    id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键',
    report_no VARCHAR(32) NOT NULL COMMENT '双人报告业务编号',
    pair_session_id BIGINT UNSIGNED NOT NULL COMMENT '配对会话ID',
    result_snapshot JSON NOT NULL COMMENT '双人聚合报告快照',
    generated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '生成时间',
    deleted TINYINT NOT NULL DEFAULT 0 COMMENT '删除标记：0正常，1删除',
    create_date DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_date DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_pair_report_no (report_no),
    UNIQUE KEY uk_pair_report_session (pair_session_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='双人聚合报告';

CREATE TABLE IF NOT EXISTS t_ai_generation_task (
    id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键',
    task_no VARCHAR(32) NOT NULL COMMENT 'AI生成任务业务编号',
    version_id BIGINT UNSIGNED NOT NULL COMMENT '生成内容所属题型版本ID',
    prompt_text TEXT NOT NULL COMMENT '运营输入的测试类型提示词',
    target_question_count SMALLINT UNSIGNED NOT NULL COMMENT '目标题库题目数',
    current_batch_no SMALLINT UNSIGNED NOT NULL DEFAULT 0 COMMENT '当前批次号，未开始为0',
    total_batch_count SMALLINT UNSIGNED NOT NULL DEFAULT 0 COMMENT '总批次数',
    completed_batch_count SMALLINT UNSIGNED NOT NULL DEFAULT 0 COMMENT '已完成批次数',
    generated_question_count SMALLINT UNSIGNED NOT NULL DEFAULT 0 COMMENT '已生成题目数',
    retry_count SMALLINT UNSIGNED NOT NULL DEFAULT 0 COMMENT '已重试次数',
    dimension_generated_flag TINYINT NOT NULL DEFAULT 0 COMMENT '维度是否已生成：1是，0否',
    result_rule_generated_flag TINYINT NOT NULL DEFAULT 0 COMMENT '结果规则是否已生成：1是，0否',
    task_status TINYINT NOT NULL DEFAULT 1 COMMENT '1待生成，2生成中，3待人工审核，4生成失败',
    error_message VARCHAR(1000) NULL COMMENT '最近一次生成失败原因',
    completed_at DATETIME NULL COMMENT '生成完成时间',
    deleted TINYINT NOT NULL DEFAULT 0 COMMENT '删除标记：0正常，1删除',
    create_date DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_date DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_ai_task_no (task_no),
    UNIQUE KEY uk_ai_task_version (version_id),
    KEY idx_ai_task_status_time (task_status, deleted, update_date),
    CONSTRAINT chk_ai_target_question_count CHECK (target_question_count > 0),
    CONSTRAINT chk_ai_batch_progress CHECK (
        current_batch_no <= total_batch_count AND completed_batch_count <= total_batch_count
    ),
    CONSTRAINT chk_ai_question_progress CHECK (generated_question_count <= target_question_count)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='AI题库生成任务';

CREATE TABLE IF NOT EXISTS t_content_audit_log (
    id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键',
    biz_type TINYINT NOT NULL COMMENT '业务类型：1题型，2题型版本，3首页配置',
    biz_id BIGINT UNSIGNED NOT NULL COMMENT '业务对象ID',
    action_type TINYINT NOT NULL COMMENT '操作类型，由业务枚举定义',
    before_snapshot JSON NULL COMMENT '操作前快照',
    after_snapshot JSON NULL COMMENT '操作后快照',
    reason VARCHAR(500) NULL COMMENT '操作原因或审核意见',
    operator_id BIGINT UNSIGNED NOT NULL COMMENT '操作人ID',
    create_date DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '操作时间',
    PRIMARY KEY (id),
    KEY idx_audit_biz_time (biz_type, biz_id, create_date),
    KEY idx_audit_operator_time (operator_id, create_date)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='内容操作审计日志';
~~~

~~~sql
CREATE TABLE IF NOT EXISTS t_analytics_visitor (
    open_id VARCHAR(128) COLLATE utf8mb4_bin NOT NULL COMMENT '微信用户OpenID',
    first_visit_at DATETIME NOT NULL COMMENT '首次访问时间',
    last_visit_at DATETIME NOT NULL COMMENT '最近访问时间',
    PRIMARY KEY (open_id),
    KEY idx_visitor_first_visit (first_visit_at),
    KEY idx_visitor_last_visit (last_visit_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='微信用户访问汇总';

CREATE TABLE IF NOT EXISTS t_analytics_event (
    id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键',
    event_id VARCHAR(64) NOT NULL COMMENT '客户端事件唯一ID，用于幂等去重',
    open_id VARCHAR(128) COLLATE utf8mb4_bin NOT NULL COMMENT '微信用户OpenID',
    session_id VARCHAR(64) NOT NULL COMMENT '小程序会话标识',
    event_type TINYINT NOT NULL COMMENT '1小程序打开，2页面浏览，3开始测试，4完成测试，5分享',
    page_path VARCHAR(255) NULL COMMENT '页面路径，不包含查询参数',
    business_id BIGINT UNSIGNED NULL COMMENT '关联题型或报告等业务ID',
    source_scene INT NULL COMMENT '微信小程序场景值',
    channel VARCHAR(32) NULL COMMENT '业务渠道',
    app_version VARCHAR(32) NULL COMMENT '小程序版本',
    client_time DATETIME NOT NULL COMMENT '客户端事件时间',
    received_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '服务端接收时间，统计以此字段为准',
    PRIMARY KEY (id),
    UNIQUE KEY uk_analytics_event_id (event_id),
    KEY idx_event_type_time (event_type, received_time),
    KEY idx_event_open_time (open_id, received_time),
    KEY idx_event_session_time (session_id, received_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='小程序访问事件';

CREATE TABLE IF NOT EXISTS t_analytics_visitor_day (
    id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键',
    stat_date DATE NOT NULL COMMENT '统计日期',
    open_id VARCHAR(128) COLLATE utf8mb4_bin NOT NULL COMMENT '微信用户OpenID',
    first_visit_at DATETIME NOT NULL COMMENT '当日首次访问时间',
    last_visit_at DATETIME NOT NULL COMMENT '当日最近访问时间',
    pv_count INT UNSIGNED NOT NULL DEFAULT 0 COMMENT '当日页面浏览次数',
    PRIMARY KEY (id),
    UNIQUE KEY uk_open_day (stat_date, open_id),
    KEY idx_visitor_day_last_visit (stat_date, last_visit_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='微信用户访问日统计';

CREATE TABLE IF NOT EXISTS t_wechat_metric_day (
    id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键',
    stat_date DATE NOT NULL COMMENT '微信统计日期',
    session_count INT UNSIGNED NOT NULL DEFAULT 0 COMMENT '打开次数',
    visit_pv INT UNSIGNED NOT NULL DEFAULT 0 COMMENT '访问PV',
    visit_uv INT UNSIGNED NOT NULL DEFAULT 0 COMMENT '访问UV',
    visit_uv_new INT UNSIGNED NOT NULL DEFAULT 0 COMMENT '新增访问UV',
    stay_time_uv DECIMAL(10,2) NULL COMMENT '人均停留时长，单位秒',
    stay_time_session DECIMAL(10,2) NULL COMMENT '次均停留时长，单位秒',
    visit_depth DECIMAL(10,2) NULL COMMENT '平均访问深度',
    sync_status TINYINT NOT NULL DEFAULT 1 COMMENT '同步状态：1成功，0失败',
    synced_at DATETIME NOT NULL COMMENT '同步时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_wechat_metric_date (stat_date)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='微信官方日访问数据';

CREATE TABLE IF NOT EXISTS t_admin_account (
    id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键',
    username VARCHAR(64) NOT NULL COMMENT '登录账号',
    password_hash VARCHAR(255) NOT NULL COMMENT '密码哈希',
    display_name VARCHAR(64) NOT NULL COMMENT '显示名称',
    role_type TINYINT NOT NULL DEFAULT 1 COMMENT '固定角色：1管理员，2内容运营，3只读查看',
    status TINYINT NOT NULL DEFAULT 1 COMMENT '状态：1启用，0禁用',
    last_login_at DATETIME NULL COMMENT '最后登录时间',
    deleted TINYINT NOT NULL DEFAULT 0 COMMENT '删除标记：0正常，1删除',
    create_date DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_date DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_admin_username (username),
    KEY idx_admin_status (status, deleted),
    CONSTRAINT chk_admin_role CHECK (role_type BETWEEN 1 AND 3)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='后台账号';

CREATE TABLE IF NOT EXISTS t_business_session (
    id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键',
    session_token_hash CHAR(64) NOT NULL COMMENT '业务会话令牌哈希，不保存明文',
    session_type TINYINT NOT NULL COMMENT '会话类型：1后台，2微信小程序',
    admin_account_id BIGINT UNSIGNED NULL COMMENT '后台账号ID',
    open_id VARCHAR(128) COLLATE utf8mb4_bin NULL COMMENT '微信用户OpenID',
    expires_at DATETIME NOT NULL COMMENT '过期时间',
    last_access_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '最后访问时间',
    revoked_at DATETIME NULL COMMENT '注销时间',
    deleted TINYINT NOT NULL DEFAULT 0 COMMENT '删除标记：0正常，1删除',
    create_date DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_date DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_business_session_token (session_token_hash),
    KEY idx_business_session_admin (admin_account_id, expires_at, deleted),
    KEY idx_business_session_open (open_id, expires_at, deleted),
    CONSTRAINT chk_business_session_subject CHECK (
        (session_type = 1 AND admin_account_id IS NOT NULL AND open_id IS NULL)
        OR (session_type = 2 AND admin_account_id IS NULL AND open_id IS NOT NULL)
    )
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='持久化业务会话';
~~~

### 8.4 实时 UV/PV 写入规则

小程序批量上报事件后，服务端按以下顺序处理：

1. 以 `event_id` 插入 t_analytics_event；唯一索引冲突表示重复上报，本次不再累计。
2. 事件首次入库成功后，新增或更新 t_analytics_visitor 的首次、最近访问时间。
3. 新增或更新 t_analytics_visitor_day；page_view 事件将 `pv_count` 加一。
4. 运营总览查询以服务端 `received_time` 为准，避免客户端时间被修改造成统计漂移。

上述三步在同一数据库事务中完成。V1 不建设消息队列和分钟聚合表；事件量明显影响查询后，再按 5 分钟维度增加聚合表，不改变小程序上报协议。

### 8.5 运营总览核心查询示例

以下 SQL 假设应用和数据库统一使用 Asia/Shanghai 时区：

题型使用次数统一指统计区间内成功创建的新答卷数，以 `t_answer_session.create_date` 为统计时间，通过 `version_id` 关联 `t_test_version.test_id`。同一 `create_request_id` 的幂等重试只返回原答卷，未完成答卷续答不创建新记录，因此均不重复计数。运营总览需展示全部未删除题型，未产生新答卷的题型使用次数显示为 0，并按使用次数倒序排列。

~~~sql
-- 今日 UV
SELECT COUNT(*) AS today_uv
FROM t_analytics_visitor_day
WHERE stat_date = CURRENT_DATE;

-- 今日 PV
SELECT COUNT(*) AS today_pv
FROM t_analytics_event
WHERE event_type = 2
  AND received_time >= CURRENT_DATE
  AND received_time < CURRENT_DATE + INTERVAL 1 DAY;

-- 今日新增访客
SELECT COUNT(*) AS today_new_uv
FROM t_analytics_visitor
WHERE first_visit_at >= CURRENT_DATE
  AND first_visit_at < CURRENT_DATE + INTERVAL 1 DAY;

-- 近 5 分钟活跃 UV
SELECT COUNT(DISTINCT open_id) AS active_uv_5m
FROM t_analytics_event
WHERE received_time >= NOW() - INTERVAL 5 MINUTE;

-- 今日 5 分钟访问趋势
SELECT FROM_UNIXTIME(FLOOR(UNIX_TIMESTAMP(received_time) / 300) * 300) AS time_bucket,
       SUM(event_type = 2) AS pv,
       COUNT(DISTINCT open_id) AS uv
FROM t_analytics_event
WHERE received_time >= CURRENT_DATE
  AND received_time < CURRENT_DATE + INTERVAL 1 DAY
GROUP BY time_bucket
ORDER BY time_bucket;

-- 统计区间内各题型成功新建答卷数，包含使用次数为 0 的题型
SELECT t.id AS test_id,
       t.test_name,
       COUNT(s.id) AS use_count
FROM t_test t
LEFT JOIN t_test_version v
       ON v.test_id = t.id
      AND v.deleted = 0
LEFT JOIN t_answer_session s
       ON s.version_id = v.id
      AND s.create_date >= :start_time
      AND s.create_date < :end_time
      AND s.deleted = 0
WHERE t.deleted = 0
GROUP BY t.id, t.test_name
ORDER BY use_count DESC, t.id DESC;
~~~

### 8.6 数据保留与结构变更要求

- t_analytics_event 默认保留 30 天，满足实时查询和问题追踪；超过期限按天批量清理。
- t_analytics_visitor_day 和 t_wechat_metric_day 长期保留，用于历史趋势和官方数据对账。
- 原始答卷保留期限、用户主动删除后的物理清理期限，需要在隐私规则确定后再配置。
- 生产表结构变更必须先更新受控 SQL 脚本并经评审后执行，禁止临时直接改表。
- 发布题型版本、切换首页焦点、答卷提交和报告生成必须由服务端事务维护，不允许后台直接改表。
